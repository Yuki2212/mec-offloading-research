# 新しい実験・解析の出力

`python -m experiments.run` は実行ごとの新しいサブフォルダにCSV、設定、環境情報を保存します。
解析ノートブックは `analysis/organized/<ノートブック名>/` 以下に図・CSV集計表・LaTeX表を保存します。解析の再実行では同じ生成物を更新します。
`analysis/` 直下などにある以前の生成物は削除せず保持しています。新しい出力は `organized/` 以下を確認してください。

保存・共有したい結果は、条件と生成手順を添えて別途選別してください。
